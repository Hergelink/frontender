import React, { useState } from 'react';
import '../../src/App.css';
import HeaderContainer from './HeaderContainer';

function Header() {

  return (
    <header>
      <HeaderContainer />
    </header>
  );
}

export default Header;
